# `.function` Directory
This directory contains files for functions.

Whilst PHP does support auto-loading classes, it does not yet support the same for functions. As a workaround, all function files are loaded, whether they are referenced in user code or not.
